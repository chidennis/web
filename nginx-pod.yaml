apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    app: nginxsss
spec:
  containers:
    - name: first-container
      image: dennislandmark25/nginx
      ports:
      - containerPort: 80
